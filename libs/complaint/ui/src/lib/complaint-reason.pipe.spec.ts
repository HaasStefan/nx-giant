import { ComplaintReasonPipe } from './complaint-reason.pipe';

describe('ComplaintReasonPipe', () => {
  it('create an instance', () => {
    const pipe = new ComplaintReasonPipe();
    expect(pipe).toBeTruthy();
  });
});
